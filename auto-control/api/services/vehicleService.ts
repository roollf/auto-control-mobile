import { VehicleData } from "@/types/vehicle/vehicle.type"
import { AxiosResponse } from "axios"
import { apiClient } from "./apiClient"

// export const VehicleService = {
//   async addVehicleExpense() {
//     return null
//   },
//   async getVehicles(userToken: string): Promise<VehicleExpenseData[]> {
//     try{
//       const response: AxiosResponse<VehicleExpenseData[]> = await apiClient.get(`/api/v1/app-vehicles/vehicles/`, {
//         headers: {
//           Authorization: `Token ${userToken}`
//         }
//       })
//     }

//   }
// }

// //trazer implementacao para esse arquivo (de criar despesa por veiculo)
